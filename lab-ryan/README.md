# Lab 11 - Single resource Express
 This application creates, reads, updates and destroys articles.

 To install, run npm i, which will install the dependencies.

Dependencies
-mongoose
-mongoDB
-express
-superagent
-mocha
-expect

 To start mongoose, just type npm start-db.
